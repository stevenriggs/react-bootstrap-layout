import React from 'react';
import {Container} from 'react-bootstrap';

function Header() {
    return (
        <Container fluid>
            Header
        </Container>
    );
}

export default Header;